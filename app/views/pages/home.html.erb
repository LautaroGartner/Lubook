<div class="max-w-2xl mx-auto mt-20 text-center">
  <h1 class="text-4xl font-bold">Lubook</h1>
  <p class="mt-4 text-gray-600">Welcome, <%= current_user.username %>.</p>
  <%= button_to "Sign out", destroy_user_session_path, method: :delete,
        class: "mt-6 px-4 py-2 bg-gray-900 text-white rounded" %>
</div>