<span id="<%= local_assigns.fetch(:badge_id, "chat_badge") %>" class="<%= count.to_i.positive? ? "inline-flex" : "hidden" %> <%= local_assigns.fetch(:badge_classes, "min-w-5 h-5 items-center justify-center rounded-full bg-stone-900 px-1.5 text-[11px] font-semibold text-white") %>">
  <%= count %>
</span>
